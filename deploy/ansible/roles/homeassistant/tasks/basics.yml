- name: Install homie specific packages
  apt: name={{item}} state=installed
  with_items:
    - nginx
    - libxslt-dev
    - libxml2-dev
    - python3
    - python3-venv
    - python3-pip
    - python3-lxml
  tags:
    - install
